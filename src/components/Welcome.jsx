import { Alert } from "react-bootstrap";

const Welcome = () => (
  <Alert className="text-center" data-testid="component-welcome">
    <h1>Benvenuti in EpiBooks!</h1>
  </Alert>
);

export default Welcome;
